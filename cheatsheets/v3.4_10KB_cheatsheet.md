You are solving equational implication problems over magmas: a set with one binary operation * and no other axioms.

Task:
Does {{ equation1 }} imply {{ equation2 }} over ALL magmas?

OUTPUT FORMAT (mandatory)
Output EXACTLY two lines and nothing else:
VERDICT: TRUE
RULE: <short rule name>
or
VERDICT: FALSE
RULE: <short rule name>

CRITICAL
- Do all analysis SILENTLY. Never print the STEPS.
- Never print chain-of-thought, calculations, or explanations.
- Follow the STEPS in order.
- Stop at the first decisive route.
- Never override an exact route with free-form algebra.
- Before using any FALSE route, check its literal premise on the written equations.
- If the premise is not exactly satisfied, reject that route.

STEP 0. INTERNAL META-CHECK
Silently:
- read both equations as binary trees
- choose one route only
- ask: what exact feature am I checking, why is it decisive, did I verify the literal premise?
If not fully verified, reject that route.

Useful terms:
- leftmost variable = first variable in the leaf-reading of a term
- rightmost variable = last variable in the leaf-reading of a term
- WORD(term) = full left-to-right list of leaves
- PARITY(term) = odd/even occurrence pattern of each variable

STEP 1. NORMALIZATION
Allowed:
- consistent variable renaming
- swapping the two sides of one equation
- dualizing BOTH equations together by reversing every product
Do not do any other simplification here.

STEP 2. EXACT TRUE ROUTES

2.1 Exact renaming
If Equation 2 is identical to Equation 1 up to consistent variable renaming:
VERDICT: TRUE
RULE: exact renaming

2.2 Tautological target
If Equation 2 is x = x:
VERDICT: TRUE
RULE: tautological target

2.3 Singleton collapse
If Equation 1 has form x = T where x does not occur anywhere in T, then Equation 1 forces a singleton magma, so every Equation 2 is true.
Also allow side-swapped form T = x.
Examples:
- x = y
- x = y*z
- x = (y*z)*w
- x = y*(z*w)
VERDICT: TRUE
RULE: singleton collapse

2.4 Constant operation
If Equation 1 is A = B where:
- A and B are both non-variable product terms, and
- the variable sets of A and B are disjoint,
then all products are forced to one constant value.
Examples:
- x*y = z*w
- (x*y)*z = u*v
VERDICT: TRUE
RULE: constant operation

2.5 Exact substitution instance
If Equation 2 is obtained from Equation 1 by one consistent substitution of variables by terms:
VERDICT: TRUE
RULE: exact substitution

2.6 Exact context instance
Write Equation 1 as L = R.
If Equation 2 is exactly C[Lσ] = C[Rσ] or C[Rσ] = C[Lσ] for one substitution σ and one one-hole context C[ ]:
VERDICT: TRUE
RULE: exact context instance

STEP 3. EXACT FALSE WITNESS ROUTES
Use only these finite-model witness routes.

3.1 Left-zero witness
Left-zero magma: a*b = a.
Every term evaluates to its leftmost variable.
This route is legal only if:
- Equation 1 has the same leftmost variable on both sides, and
- Equation 2 has different leftmost variables on its two sides.
Then:
VERDICT: FALSE
RULE: left-zero witness

3.2 Right-zero witness
Right-zero magma: a*b = b.
Every term evaluates to its rightmost variable.
This route is legal only if:
- Equation 1 has the same rightmost variable on both sides, and
- Equation 2 has different rightmost variables on its two sides.
Then:
VERDICT: FALSE
RULE: right-zero witness

3.3 XOR parity witness
On {0,1} with a*b = a+b mod 2, each side depends only on variable parity.
This route is legal only if:
- Equation 1 has the same parity pattern on both sides, and
- Equation 2 has different parity patterns on its two sides.
Then:
VERDICT: FALSE
RULE: XOR parity witness

FORBIDDEN FALSE ROUTES
- Do not use WORD as a standalone FALSE theorem.
- Do not use leftmost, rightmost, or parity approximately.
- Do not claim a witness unless its premise is exactly satisfied on the written equations.

STEP 4. TINY EXACT REWRITE
Use this only if STEPs 2-3 did not decide.

Write Equation 1 as L = R.
Allowed move:
- choose one consistent substitution σ
- choose one one-hole context C[ ]
- replace one exact occurrence of Lσ by Rσ, or Rσ by Lσ, inside C[ ]

Restrictions:
- at most 2 rewrite steps total
- exact subterm replacement only
- no invented lemmas
- no analogical rewriting

If you reach Equation 2 exactly within at most 2 steps:
VERDICT: TRUE
RULE: exact rewrite

STEP 5. FINAL FALLBACK
If no earlier route fired:
VERDICT: FALSE
RULE: default false

FINAL REMINDERS
- Output exactly two lines.
- Never print the STEPS.
- RULE must never be blank.
- If no exact route fires, default false.
